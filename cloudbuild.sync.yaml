steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'gcr.io/$PROJECT_ID/stadianav-sync',
      '--build-arg', 'DATABASE_URL=$_DATABASE_URL',
      '-f', 'Dockerfile.sync',
      '.'
    ]
images:
  - 'gcr.io/$PROJECT_ID/stadianav-sync'
